<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- MainActivity theme. -->
    <style name="ForecastTheme" parent="AppTheme">
        <!-- Override the actionBarStyle -->
        <item name="actionBarStyle">@style/ActionBar.Solid.ForecastActionBar.NoTitle</item>
    </style>

    <!-- SettingsActivity theme. -->
    <style name="SettingsTheme" parent="AppTheme">
    </style>

    <!-- MainActivity action bar styles -->
    <style name="ActionBar.Solid.ForecastActionBar.NoTitle" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="displayOptions">useLogo|showHome</item>
        <item name="logo">@drawable/ic_logo</item>
        <item name="android:onClick">onClickLogo</item>
    </style>

    <!-- Style for forecast listview -->
    <style name="ForecastListStyle">
        <!-- Here it's a stub.  In screens of sufficient width, this style includes modifications
        for two-pane layout -->
    </style>

</resources>
